# CreativeHub: Portfólio de Design Fictício

Este é um projeto de front-end simples que cria uma página de galeria para um portfólio de design fictício chamado "CreativeHub". O objetivo principal do projeto foi aplicar e demonstrar conceitos fundamentais de CSS para estruturação, estilização e layout responsivo.

## 📸 Visualização do Projeto

Abaixo está uma captura de tela do projeto finalizado.

![Visualização do Projeto CreativeHub](image_bfbadd.jpg)

## ✨ Funcionalidades

-   **Galeria de Projetos:** Exibe 3 projetos fictícios em um layout limpo.
-   **Layout Responsivo com Rolagem Horizontal:** Em qualquer tamanho de tela, os itens da galeria permanecem em uma única linha, que pode ser rolada horizontalmente.
-   **Estrutura Semântica:** Utiliza tags HTML5 (`<header>`, `<main>`, `<section>`, `<footer>`) para uma melhor organização e acessibilidade.
-   **Estilização Moderna:** Design minimalista com foco na apresentação visual dos projetos.

## 🚀 Tecnologias Utilizadas

O projeto foi construído utilizando as seguintes tecnologias:

-   **HTML5:** Para a estrutura e o conteúdo da página.
-   **CSS3:** Para toda a estilização, layout e responsividade.

## 💡 Conceitos de CSS Aplicados

Este projeto foi um exercício prático para reforçar os seguintes conceitos de CSS:

-   **Modelo de Caixa (Box Model):** Uso intensivo de `margin`, `padding` e `border` para criar espaçamento e estrutura nos cartões da galeria.
-   **Flexbox:** Aplicação de `display: flex` para criar o layout da galeria. Especificamente:
    -   `flex-wrap: nowrap` para forçar os itens a permanecerem em uma única linha.
    -   `overflow-x: auto` para habilitar a rolagem horizontal quando o conteúdo excede o tamanho da tela.
-   **Estilização de Texto:** Utilização de propriedades como `font-size`, `font-weight` e `text-align` para garantir a hierarquia visual e a legibilidade.
-   **Estilização de Imagens:**
    -   `border-radius` para criar cantos arredondados.
    -   `object-fit: cover` para garantir que todas as imagens tenham um tamanho uniforme sem distorção.

## ⚙️ Como Executar o Projeto

Como este é um projeto front-end estático, não há necessidade de instalação de dependências.

1.  **Clone o repositório:**
    ```bash
    git clone [https://github.com/seu-usuario/creativehub.git](https://github.com/seu-usuario/creativehub.git)
    ```
2.  **Navegue até a pasta do projeto:**
    ```bash
    cd creativehub
    ```
3.  **Abra o arquivo `index.html`** no seu navegador de preferência.

E pronto! A página da galeria será exibida.
